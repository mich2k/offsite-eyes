<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.ooproject.offsiteeyes">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Offsiteeyes.NoActionBar">
        <activity
            android:name=".activities.MyRecipeActivity"
            android:label="MyMealsActivity"
            android:theme="@style/Theme.Offsiteeyes.NoActionBar" />
        <activity
            android:name=".activities.PantryActivity"
            android:label="@string/title_activity_service_my_recipes"
            android:theme="@style/Theme.Offsiteeyes.NoActionBar"/>
        <activity
            android:name=".activities.HomeActivity"
            android:label="@string/title_activity_scrolling_calcs"
            android:theme="@style/Theme.Offsiteeyes.NoActionBar" />

        <activity
            android:name=".activities.ToolsActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".BootLogoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>